#!/bin/sh

if [ "$CHANDLERHOME" = "" ]
then
    CHANDLERHOME=$(dirname "$(dirname "${0}")")
fi

if [ "$CHANDLERBIN" = "" ]
then
    CHANDLERBIN=$CHANDLERHOME
fi

$CHANDLERBIN/release/RunPython $CHANDLERHOME/Chandler.py "$@"
