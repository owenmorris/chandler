--- ZopeInterface-3.0.0b1/Support/zpkgtools/setup.py	2004-06-11 12:04:17.000000000 -0700
+++ ZopeInterface-3.0.0b1-patched/Support/zpkgtools/setup.py	2005-03-01 23:30:44.000000000 -0800
@@ -75,28 +75,20 @@
         for name in self.__dict__:
             if name[0] == "_":
                 del kwargs[name]
-        if "--debug" in sys.argv:
-            import pprint
-            try:
-                pprint.pprint(kwargs)
-            except IOError, e:
-                if e.errno != errno.EPIPE:
-                    raise
-        else:
-            if sys.version_info < (2, 3):
-                from distutils.dist import DistributionMetadata
-                DistributionMetadata.classifiers = None
-                DistributionMetadata.download_url = None
-            try:
-                from setuptools import setup
-            except ImportError:
-                # package_data can't be handled this way ;-(
-                if self.package_data:
-                    print >>sys.stderr, (
-                        "can't import setuptools;"
-                        " some package data will not be properly installed")
-                from distutils.core import setup
-            setup(**kwargs)
+        if sys.version_info < (2, 3):
+            from distutils.dist import DistributionMetadata
+            DistributionMetadata.classifiers = None
+            DistributionMetadata.download_url = None
+        try:
+            from setuptools import setup
+        except ImportError:
+            # package_data can't be handled this way ;-(
+            if self.package_data:
+                print >>sys.stderr, (
+                    "can't import setuptools;"
+                    " some package data will not be properly installed")
+            from distutils.core import setup
+        setup(**kwargs)
 
     def load_metadata(self, path):
         f = open(path, "rU")
--- ZopeInterface-3.0.0b1/Dependencies/zope.interface-ZopeInterface-3.0.0b1/zope.interface/_zope_interface_coptimizations.c	2004-06-02 06:17:31.000000000 -0700
+++ ZopeInterface-3.0.0b1-patched/Dependencies/zope.interface-ZopeInterface-3.0.0b1/zope.interface/_zope_interface_coptimizations.c	2005-05-04 21:16:26.000000000 -0700
@@ -334,7 +334,7 @@
 	{NULL,		NULL}		/* sentinel */
 };
 
-static PyTypeObject SpecType = {
+PyTypeObject SpecType = {
 	PyObject_HEAD_INIT(NULL)
 	/* ob_size           */ 0,
 	/* tp_name           */ "_interface_coptimizations."
