"""
  All parcels must define parcelClass in their __init__.py to tell the
parcel loader which class defines the parcel. It is a string of the
form "file.class", where file is the name of a python file (without the
.py extension) and class is the class within the python file that is
the parcel class.
"""

parcelClass = "DocumentViewer.DocumentViewer"
